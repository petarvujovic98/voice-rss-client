import { getSpeech, makeRequest, validate } from "../src";

describe("validate", () => {
  it("should be defined", () => {
    expect(validate).toBeDefined();
  });
});

describe("makeRequest", () => {
  it("should be defined", () => {
    expect(makeRequest).toBeDefined();
  });
});

describe("getSpeech", () => {
  it("should be defined", () => {
    expect(getSpeech).toBeDefined();
  });
});
